# driver.transferline.uk
# Serves Next.js (driver panel)

server {
  listen 80;
  server_name driver.transferline.uk;

  location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
  }

  location / { return 301 https://$host$request_uri; }
}

server {
  listen 443 ssl http2;
  server_name driver.transferline.uk;

  ssl_certificate     /etc/letsencrypt/live/driver.transferline.uk/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/driver.transferline.uk/privkey.pem;
  include /etc/nginx/snippets/ssl-params.conf;

  limit_conn conn_limit_per_ip 30;

  # PWA/service worker must not be cached incorrectly
  location = /service-worker.js {
    proxy_pass http://host.docker.internal:3000;
    add_header Cache-Control "no-cache" always;
  }

  location / {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Connection "";

    proxy_pass http://host.docker.internal:3000;
  }
}
