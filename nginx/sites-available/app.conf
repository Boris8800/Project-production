# transferline.uk + www
# Serves Next.js (customer booking)
# Frontend runs on the host (outside Docker) and is reverse-proxied by this nginx container.

server {
  listen 80;
  server_name transferline.uk www.transferline.uk;

  # ACME challenge
  location ^~ /.well-known/acme-challenge/ {
    root /var/www/certbot;
    default_type "text/plain";
  }

  location / {
    return 301 https://$host$request_uri;
  }
}

server {
  listen 443 ssl http2;
  server_name transferline.uk www.transferline.uk;

  ssl_certificate     /etc/letsencrypt/live/transferline.uk/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/transferline.uk/privkey.pem;
  include /etc/nginx/snippets/ssl-params.conf;

  limit_conn conn_limit_per_ip 30;

  # Static asset caching (Next)
  location ^~ /_next/static/ {
    proxy_pass http://host.docker.internal:3000;
    proxy_cache STATIC;
    proxy_cache_valid 200 7d;
    add_header Cache-Control "public, max-age=604800, immutable" always;
  }

  location ^~ /assets/ {
    proxy_pass http://host.docker.internal:3000;
    add_header Cache-Control "public, max-age=86400" always;
  }

  location / {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Connection "";

    proxy_pass http://host.docker.internal:3000;
  }
}
