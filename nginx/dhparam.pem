# This file is intentionally a placeholder.
# In production, DH params are stored in the Let's Encrypt volume at:
#   /etc/letsencrypt/ssl-dhparams.pem
# and created by bash scripts/all.sh setup-ssl on first deploy.
